# Minimal dependencies for the Smart-Traffic-RL code repo.
# Versions are loosely pinned for now; exact versions get frozen into the repro
# contract (`data_version`) once it is active. See vault: notes/research-eval-repro.
#
# NOTE: `torch` is intentionally NOT pinned here. The CUDA build must be installed
# deliberately with the correct index URL for the GPU. From T-02-09 (pilot) on it is
# required: CPU build `pip install torch --index-url https://download.pytorch.org/whl/cpu`
# for benchmarking / the small LSTM; swap to the matching CUDA build for real training.
# See README -> "GPU / torch".

traci
sumolib
libsumo==1.27.0  # in-process SUMO backend (LIBSUMO_AS_TRACI=1); pin to the SUMO version
numpy
gymnasium
pyyaml          # load specs/movements.yaml for the network-build assertion (T-01-02)
sqlalchemy      # SQLite (WAL) results schema + ORM (T-01-03)

# Evaluation / analysis phase (T-04-02): required by scripts/analyze_eval.py,
# scripts/build_analysis.py and `make eval-final`.
scipy           # Wilcoxon signed-rank (preregistration.md s5)
pandas          # table assembly in scripts/build_analysis.py
matplotlib      # plots P1-P5
nbformat        # analysis/final_eval.ipynb is built programmatically
nbconvert       # `make eval-final` re-executes the notebook in place
ipykernel       # the python3 kernel the notebook executes against

# Presentation layer (T-05-01/04): the FastAPI hub, its WebSocket channels and REST replay.
fastapi         # the hub itself (src/api/server.py)
uvicorn         # ASGI server: python -m uvicorn src.api.server:app
websockets      # WebSocket protocol support for uvicorn
httpx           # test client transport (TestClient) - test-only, but import-time required
pytest-asyncio  # the async hub/backpressure tests
